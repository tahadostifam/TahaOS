{
  home.file = {
    ".config/helix/themes/skull.toml" = {
        source = ./skull.toml;
        recursive = true;
    };

    ".config/helix/config.toml" = {
        source = ./config.toml;
        recursive = true;
    };
  }; 
}
